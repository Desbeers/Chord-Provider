# ``ChordProviderMIDI``

@Metadata {
    @DisplayName("Chord Provider MIDI")
    @PageImage(purpose: card, source: card-midi, alt: Icon)
}

Play *Chords*, *Grids* and *Tabs* with MIDI

## Overview

**Chord Provider** can play *Chords*, *Grids* and *Tabs* with MIDI.

The {tempo} and {time} *directives* will be used if set.
