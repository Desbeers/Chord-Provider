# ``ChordProviderCore``

@Metadata {
    @DisplayName("Chord Provider Core")
    @PageImage(purpose: card, source: card-large-icon-app, alt: Icon)
}

The core package for **Chord Provider**

## Overview

The core package for **Chord Provider** for parsing *ChordPro* songs into a JSON structure that can be rendered in a GUI.
