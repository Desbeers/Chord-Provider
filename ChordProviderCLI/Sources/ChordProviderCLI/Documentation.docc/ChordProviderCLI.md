# ``chordprovider``

@Metadata {
    @DisplayName("Chord Provider CLI")
    @PageImage(purpose: card, source: card-cli, alt: Icon)
}

**Chord Provider** for the Command Line

## Overview

**Chord Provider** for parsing *ChordPro* songs into a JSON structure from the Command Line.