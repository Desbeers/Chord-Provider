# ``ChordProviderCLI``

@Metadata {
    @DisplayName("Chord Provider CLI")
    @PageImage(purpose: card, source: card-large-cli, alt: Icon)
}

**Chord Provider** for the Command Line

## Overview

This is the **Chord Provider** *CLI* version for parsing *ChordPro* songs into a JSON structure from the Command Line.

![Screenshot](card-large-cli.png)

#### For more info and options:
``` bash
chordprovider --help
```
