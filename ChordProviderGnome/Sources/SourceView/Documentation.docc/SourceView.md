# ``SourceView``

@Metadata {
    @DisplayName("Chord Provider Editor")
    @PageImage(purpose: card, source: card-editor, alt: Icon)
}

The `GtkSourceView` editor for **Chord Provider**

## Overview

**Chord Provider** is using the `GtkSourceView` component as its editor in the GUI.
