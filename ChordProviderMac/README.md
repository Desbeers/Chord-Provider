#  Chord Provider for macOS

No longer supported...

It does not build anymore.

macOS Tahoe is ugly and even more lockdown than before.

There was a time Apple liked `Open Source` but not anymore.

`ChordProviderGnome` compiles on macOS with the correct stuff added with `homebrew`.

